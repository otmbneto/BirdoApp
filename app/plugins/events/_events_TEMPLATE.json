{	
	"project": "{PROJECT_PREFIX}",
	"description": "eventos de publish e render do projeto {PROJETO}. Pega cenas publicadas manualmente na lista de folders a seguir, e publica como se fosse a fazendinha",
	"render_scenes": '"LOCAL" ou "FAZENDINHA"',
	"publish_events": {
		"log_folder": "",
		"temp_folder": "",
		"render_type": '"pre_comp" ou "comp"',
		"step": '"SETUP", "ANIM" ou "COMP"',
		"folder_targets": {
			"EPXXX": "FOLDER/PATH"
		}
	}
}